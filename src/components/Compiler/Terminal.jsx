import React from 'react';
import { Box } from '@mui/material';

function Terminal() {
  return (
    <Box
      sx={{
        width: '30%',
        height: '100%',
        bgcolor: '#2B3245',
        padding: '0.7rem',
        borderRadius: '20px'
      }}
    >
        Terminal
    </Box>
  );
}

export default Terminal;
